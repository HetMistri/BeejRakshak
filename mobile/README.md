# BeejRakshak Mobile (React Native)

UI-only mobile prototype built with Expo.

## Run

1. Install dependencies:
   - `npm install`
2. Start:
   - `npm run start`

> Uses the Expo Go app for device previews.
